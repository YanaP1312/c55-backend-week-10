spring:
  application:
    name: security
  datasource:
    url: '${DB_URL}'
    username: '${DB_USERNAME}'
    password: '${DB_PASSWORD}'
  sql:
    init:
      mode: always

jwt:
  secret: '${JWT_SECRET}'
  expiration-ms: 900000
